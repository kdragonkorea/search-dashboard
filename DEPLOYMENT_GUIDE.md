# 🚀 GitHub & Streamlit Cloud 배포 가이드

## 📌 1단계: GitHub에 코드 푸시하기

### 1-1. GitHub 레포지토리 연결
```bash
# 현재 디렉토리에서 실행
cd "/Users/hana/Documents/99_coding/04_Search Trends  Dashboard"

# GitHub 레포지토리 URL로 원격 저장소 추가 (예시)
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git

# 원격 저장소 확인
git remote -v
```

### 1-2. GitHub에 푸시
```bash
# main 브랜치로 푸시
git push -u origin main
```

⚠️ **주의사항:**
- `.gitignore`에 `*.parquet`, `data_storage/`, `*.csv` 등 데이터 파일이 포함되어 있어 대용량 파일은 업로드되지 않습니다.
- Private 레포지토리이므로 외부에서 코드가 보이지 않습니다.

---

## 📌 2단계: Streamlit Cloud 배포하기

### 2-1. Streamlit Cloud 접속
1. [https://share.streamlit.io/](https://share.streamlit.io/) 접속
2. GitHub 계정으로 로그인

### 2-2. 새 앱 배포
1. **"New app"** 버튼 클릭
2. 다음 정보 입력:
   - **Repository**: `YOUR_USERNAME/YOUR_REPO_NAME`
   - **Branch**: `main`
   - **Main file path**: `app.py`
3. **"Deploy!"** 클릭

### 2-3. 배포 진행 상황 확인
- 배포는 약 3-5분 소요됩니다.
- 로그에서 설치 진행 상황을 확인할 수 있습니다.

---

## 📌 3단계: 샘플 데이터 생성 (필수!)

Streamlit Cloud에는 실제 데이터가 없으므로, 앱 시작 시 자동으로 샘플 데이터를 생성하도록 설정되어 있습니다.

### 현재 설정 상태:
- `app.py`에서 `data_storage/` 폴더에 `.parquet` 파일이 없으면 자동으로 샘플 데이터를 생성합니다.
- `generate_data.py`를 사용하여 더미 데이터를 생성합니다.

---

## 📌 4단계: Private 레포지토리 배포 설정

### Private 레포지토리 권한 설정:
1. Streamlit Cloud 대시보드에서 **Settings** 클릭
2. **Sharing** 탭에서 접근 권한 설정:
   - **Public**: 누구나 접근 가능 (권장하지 않음)
   - **Private**: 이메일로 초대된 사용자만 접근 가능
   - **Password-protected**: 비밀번호로 보호

---

## 📌 5단계: 배포 완료 후 확인사항

### 확인할 사항:
✅ 앱이 정상적으로 로드되는지  
✅ 탭 텍스트가 잘 보이는지  
✅ 파이차트가 잘리지 않고 표시되는지  
✅ 테마 전환(Light/Dark)이 정상 작동하는지  
✅ 표에서 'NEW' 행 하이라이트가 표시되는지

### 배포 URL:
배포 완료 후 다음 형식의 URL이 생성됩니다:
```
https://YOUR_APP_NAME.streamlit.app
```

---

## 🔧 문제 해결

### 문제 1: "ModuleNotFoundError"
**원인**: `requirements.txt`에 필요한 패키지가 누락됨  
**해결**: `requirements.txt`에 패키지 추가 후 재배포

### 문제 2: "FileNotFoundError: data_storage/"
**원인**: 데이터 파일이 없음  
**해결**: `app.py`의 자동 샘플 데이터 생성 로직이 작동하는지 확인

### 문제 3: "Font not found"
**원인**: `assets/JOURNEYITSELF-REGULAR 3.TTF` 경로 문제  
**해결**: 폰트 파일이 `assets/` 폴더에 있는지 확인 (이미 포함되어 있음)

---

## 📝 추가 설정 (선택사항)

### 커스텀 도메인 연결:
1. Streamlit Cloud 대시보드에서 **Settings** > **General**
2. **Custom subdomain** 입력 (예: `my-dashboard`)
3. URL이 `https://my-dashboard.streamlit.app`으로 변경됨

### 환경 변수 설정:
1. **Settings** > **Secrets**
2. TOML 형식으로 환경 변수 추가:
```toml
[general]
api_key = "your_api_key_here"
```

---

## 🎉 완료!

이제 웹에서 대시보드를 확인할 수 있습니다!
