# Streamlit Secrets 설정
# 이 파일은 로컬 개발용입니다.
# Streamlit Cloud에서는 Settings > Secrets에서 동일한 내용을 입력하세요.

# Google Drive 파일 설정
[gdrive."data_20261001_20261130.parquet"]
file_id = "1Ly6-c0JYpiWGTx56-QHQdzAEZI6axf-i"
enabled = true

# 사용 방법:
# 1. Google Drive에 data_20261001_20261130.parquet 파일 업로드
# 2. 파일 우클릭 → "공유" → "링크가 있는 모든 사용자"로 설정
# 3. 링크 복사 (예: https://drive.google.com/file/d/1ABC123XYZ/view?usp=sharing)
# 4. 링크에서 파일 ID 추출 (1ABC123XYZ 부분)
# 5. 위의 file_id에 파일 ID 입력
# 6. enabled = true로 변경
